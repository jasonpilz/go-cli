# TEMPLATE_TODO: Replace instances of 'cli' with binary named in .goreleaser.yml (builds.binary)
FROM alpine:3.10

RUN apk add --update \
        bash \
        curl \
        ca-certificates \
        && rm -rf /var/cache/apk/*

WORKDIR /app

COPY cli .

ENTRYPOINT ["./cli"]
